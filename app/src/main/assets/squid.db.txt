CREATE TABLE `observe_data` (
	`_id`	INTEGER PRIMARY KEY AUTOINCREMENT,
	`global_id`	TEXT NOT NULL UNIQUE,
	`observe_date`	TEXT NOT NULL,
	`latitude`	REAL,
	`longitude`	REAL,
	`user_id`	TEXT,
	`uploaded`	INTEGER,
	`data`	TEXT
);